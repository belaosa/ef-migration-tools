# EF Migration Script Configuration
# Copy this file to .env and update with your project paths

# Repository root path (absolute path to your solution)
REPO_PATH=C:\Users\username\source\repos\project-name

# Project containing migrations (relative to REPO_PATH)
PROJECT_NAME=ProjectName.Data

# Startup project for EF Core (relative to REPO_PATH)
STARTUP_PROJECT=ProjectName.Data

# Migrations directory (relative to REPO_PATH)
MIGRATIONS_DIR=ProjectName.Data\Migrations

# Scripts output directory (relative to REPO_PATH)
SCRIPTS_DIR=ProjectName.Data\Scripts

# Optional: DbContext name (only if you have multiple contexts)
# DBCONTEXT_NAME=MyDbContext
